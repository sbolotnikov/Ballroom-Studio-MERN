import React, {Fragment, useEffect} from 'react';

function Login(){
    useEffect( () => {
        console.log("on-load");
    }, []);

    return (
        <Fragment>

        </Fragment>
    )
}

export default Login;